export default function FloatCard(producto) {
    <div>
        <img>{producto.img}</img>
        <p>{producto.cantidad}</p>
        <p>{producto.marca}</p>
        <p>{producto.tipo}</p>
        <p>{producto.precio}</p>
    </div>
}